import { StyleSheet, } from 'react-native'
import ServiceDetailsScreen from './ServiceScreen'

const styles = StyleSheet.create({
    container: {
        backgroundColor: 'red',
        marginTop: 150,
    },
})